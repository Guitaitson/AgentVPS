name: check_redis
description: "Verifica conexão e status do Redis. Use quando perguntarem sobre cache ou Redis."
version: "1.0.0"
security_level: safe
triggers:
  - redis
  - cache
  - /health
parameters: {}
parameters_schema: {}
max_output_chars: 500
timeout_seconds: 10
enabled: true
