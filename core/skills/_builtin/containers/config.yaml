name: list_containers
description: "Lista containers Docker em execução no servidor. Use quando perguntarem sobre containers ou Docker."
version: "1.0.0"
security_level: safe
triggers:
  - containers
  - docker
  - /containers
  - listar containers
  - quais containers
parameters: {}
parameters_schema: {}
max_output_chars: 2000
timeout_seconds: 15
enabled: true
